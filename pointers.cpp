#include <iostream>

// Program entry point
int main() {
  int foo = 5;        // Variable 'foo' with a value of 5
  int* ptr = &foo;    // Pointer variable holding the memory address of the 'foo' variable
}
